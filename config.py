from pathlib import Path

# Change this path to where your dataset folder is stored
DATASET_DIR = Path("dataset")

# Where EDA outputs will be saved
EDA_OUTPUT_DIR = Path("outputs/eda")

# Image settings
IMG_SIZE = (224, 224)
BATCH_SIZE = 16
RANDOM_STATE = 123
